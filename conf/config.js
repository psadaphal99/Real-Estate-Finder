
const dbConfig = {
    userName: "root",
    password: "Abc@1234",
    port: 3306,
    database: "real_estate_db",
    host: "localhost"
}

const startConfig = {
    serverPort:8080
}

module.exports = {dbConfig, startConfig};